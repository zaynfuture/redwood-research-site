# Project instructions

- Use Stock Analysis Local Knowledge before external sources: check knowledge health, search the relevant local collections, and build a context bundle capped at 1,500 estimated tokens. Preserve evidence identifiers and list gaps before using external sources.
- Keep every completed website change synchronized in `/Users/zaynfuture/Developments/redwood-site`.
- The user uploads this repository to GitHub manually. Do not push to GitHub unless the user explicitly requests it.
- Cloudflare deployment does not replace the requirement to preserve the same source changes in the local repository.
